{{> header icon="⏰" title="Payment Deadline Reminder" subtitle=(concat daysRemaining " day(s) remaining") style=(concat "background-color: " (if (lte daysRemaining 1) "#e74c3c" "#f39c12") ";")}}

<div class="content">
  <h2>Dear {{recipientName}},</h2>

  {{#> details-box backgroundColor="#fef5e7" borderColor=(if (lte daysRemaining 1) "#e74c3c" "#f39c12")}}
    <h3>⏰ Urgent: Payment Deadline Approaching</h3>
    <p>This is a reminder that your <strong>{{#if (eq paymentType 'deposit')}}Deposit Payment{{else}}Final Payment{{/if}}</strong> for auction <strong>{{auctionCode}}</strong> is due soon.</p>
    <p><strong>Amount Due:</strong> <span style="font-size: 24px; font-weight: bold;">{{formatCurrency amountDue}} VND</span></p>
    <p><strong>Deadline:</strong> <span style="color: {{#if (lte daysRemaining 1)}}#e74c3c{{else}}#f39c12{{/if}}; font-weight: bold;">{{formatDate deadline}}</span></p>
    <p><strong>Time Remaining:</strong> <span style="color: {{#if (lte daysRemaining 1)}}#e74c3c{{else}}#f39c12{{/if}}; font-weight: bold;">{{daysRemaining}} day(s)</span></p>
  {{/details-box}}

  {{> button url=paymentUrl text="Complete Payment Now" variant=(if (lte daysRemaining 1) "danger" "warning")}}

  {{#> details-box title="📋 Auction Details"}}
    <p><strong>Auction:</strong> {{auctionName}}</p>
    <p><strong>Auction Code:</strong> {{auctionCode}}</p>
  {{/details-box}}

  <h3>⚠️ Important Warning</h3>
  {{#if (eq paymentType 'deposit')}}
  <p>If payment is not received by the deadline, your registration will be <strong>automatically cancelled</strong> and you will not be able to participate in this auction.</p>
  {{else}}
  <p>If payment is not received by the deadline:</p>
  <ul>
    <li>Your deposit will be <strong>forfeited</strong></li>
    <li>You will lose the right to purchase the property</li>
    <li>The property may be offered to the second-highest bidder</li>
  </ul>
  {{/if}}

  <h3>📞 Need Help?</h3>
  <p>If you're experiencing difficulties with payment, please contact our support team immediately:</p>
  <ul>
    <li>Email: support@auctionhub.com</li>
    <li>Phone: [Your phone number]</li>
  </ul>

  {{> footer message=(concat "Act now to avoid missing the deadline! Payment deadline: " (formatDate deadline))}}
</div>
