# NPM configuration optimized for Windows Docker Desktop
# Dramatically reduce concurrent connections (Docker Desktop networking bottleneck)
maxsockets=1

# Aggressive retries with shorter timeouts
fetch-retries=10
fetch-retry-factor=3
fetch-retry-maxtimeout=60000
fetch-retry-mintimeout=5000

# Overall timeout settings
timeout=120000

# Reduce verbosity
loglevel=error
